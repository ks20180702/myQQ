聊天消息实现。

1.增加userNotRecvMsgNum(用户和好友间未读消息数量类)。用户登录时，会返回该用户在离线时其他好友
    发送的消息数量，当用户点击好友时(查看消息并聊天时)，客户端发送查看消息的请求，服务器端返回
    具体消息内容，并设置数据库参数表示他们之前不存在未读消息。

2.用户登录时，返回所有未读消息(包括消息内容)，用户点击时，直接在本地对象中读取。用户查看后会发送
    一条该好友信息已读的指令，服务器端修改数据库标记。

3.最优但麻烦的方法。使用本地文件记录消息内容，这样还可以查看历史记录。


考虑决定使用第2种方式，第一种可能出现提示有消息但点进去没有消息的情况(消息内容对象传送中丢失)



**********
好友发送消息时的接收处理。

1.客户A和客户B登录时，服务器端回记录账号和sockAddr的map。客户A发送消息到服务器端，服务器处理后发现
    需要发送给客户B。则从map中回去对应的sockAddr，然后发送过去。
    (无法实现)，因为端口每次都是临时的，map中记录的是开始的端口，一段时间后就失效了。
    (理论可行的解决办法)，客户端发送和接收为分开的sock，**接收端也绑定一个端口，且发送给服务器端，服务器端通
        过该端口发送数据回客户端。
    (失败)解决方法暂时做不到，由于家里wifi无法登录光猫后台，无法映射端口出去，所以绑定不了。
        测试绑定局域网的端口(显然没屁用，需绑定一个外网端口然后映射到本地局域网端口)

2.只能使用心跳包带回聊天记录，会有一些通信延迟。

只能使用方法2，方式1显然更符合实际，但暂时完善不了。
